import { View, Text, ScrollView } from 'react-native'
import React from 'react'

const Form = () => {
    return (
        <ScrollView>
            <Text style={{ fontSize: 90 }}>Form</Text>
            <Text style={{ fontSize: 90 }}>Form</Text>
            <Text style={{ fontSize: 90 }}>Form</Text>
            <Text style={{ fontSize: 90 }}>Form</Text>
        </ScrollView>
    )
}

export default Form